@using IMS.Domain.Relationship
@model WarehouseProducts


@{
    ViewData["Title"] = "AddProductInWarehouse";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h1>Create</h1>

<h4>Product</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddProductInWarehouse" asp-controller="Products" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="WarehouseProductId" />
            <div class="form-group">
                <label asp-for="WarehouseProduct.ProductName" class="control-label"></label>
                <input asp-for="WarehouseProduct.ProductName" class="form-control" type="text" disabled />
            </div>
            
            <div class="form-group">
                <label class="control-label">Warehouses</label>
                <select asp-for="WarehouseId" class="form-control"
                        asp-items="ViewBag.Warehouses"
                        id="WarehouseId"></select>
            </div>
            <div class="form-group">
                <label asp-for="QuantityInStock" class="control-label"></label>
                <input asp-for="QuantityInStock" class="form-control" type="number" />
            </div>
            <div class="form-group">
                <label asp-for="ReorderLimit" class="control-label"></label>
                <input asp-for="ReorderLimit" class="form-control" type="number" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}